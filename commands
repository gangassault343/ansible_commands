Using Public Key
ssh-copy-id -f "-o IdentityFile <PATH TO PEM FILE>" ubuntu@<INSTANCE-PUBLIC-IP>
  
ssh-copy-id: This is the command used to copy your public key to a remote machine.
-f: This flag forces the copying of keys, which can be useful if you have keys already set up and want to overwrite them.
"-o IdentityFile ": This option specifies the identity file (private key) to use for the connection. The -o flag passes this option to the underlying ssh command.
ubuntu@: This is the username (ubuntu) and the IP address of the remote server you want to access.

Using Password
Go to the file /etc/ssh/sshd_config.d/60-cloudimg-settings.conf
Update PasswordAuthentication yes
Restart SSH -> sudo systemctl restart ssh
ssh-keygen -t rsa -b 4096
ssh-copy-id ubuntu@65.0.26.40

#Command for playbook execution
ansible-playbook -i inventory.inf ansible-playbook.yaml

#One-line command for installation of Nginx
ansible app -i inventory.inf -m shell -a 'apt update && apt install -y nginx && systemctl start nginx && systemctl enable nginx'
# As a root user
ansible app -i inventory.inf -m shell -a 'apt update && apt install -y nginx && systemctl start nginx && systemctl enable nginx' --become

# Creating ansible role using ansible galaxy
ansible-galaxy role init dictionary

# Import your github code to ansible-galaxy
ls ~/.ansible/roles
ansible-galaxy import gangassault343 <project-name> -- token <create token in ansible galaxy>

# Install AWS Collection
ansible-galaxy collection install amazon.aws
# Setup Vault: Create a password for vault
openssl rand -base64 2048 > vault.pass
# Add your AWS credentials using the below vault command
ansible-vault create group_vars/all/pass.yml --vault-password-file vault.pass (aws_access_key: AKIxxxxxxxxxx aws_secret_key: Uxxxxxxxxxxx)

#Create EC2 instance
ansible-playbook -i inventory.inf ec2-create.yaml --vault-password-file vault.pass

